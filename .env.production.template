# Template zmiennych środowiskowych dla Render.com
# Skopiuj te zmienne do panelu Environment Variables w Render

# === WYMAGANE ZMIENNE ===

# Baza danych (PostgreSQL na Render)
DATABASE_URL=postgresql://username:password@hostname:port/database

# JWT Secret (wygeneruj silny klucz)
JWT_SECRET=your-super-strong-jwt-secret-key-here

# NextAuth Secret (wygeneruj silny klucz)
NEXTAUTH_SECRET=your-super-strong-nextauth-secret-key-here

# URL aplikacji (zamień na swój URL Render)
NEXTAUTH_URL=https://your-app-name.onrender.com

# Środowisko
NODE_ENV=production

# === OPCJONALNE ZMIENNE (OAuth) ===

# Google OAuth (jeśli używasz)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Facebook OAuth (jeśli używasz)  
FACEBOOK_CLIENT_ID=your-facebook-client-id
FACEBOOK_CLIENT_SECRET=your-facebook-client-secret

# === INSTRUKCJE ===

# 1. Wygeneruj silne klucze:
#    openssl rand -base64 32

# 2. W Render, przejdź do:
#    Dashboard > Your Service > Environment

# 3. Dodaj każdą zmienną osobno jako Key-Value pair

# 4. Nie dodawaj komentarzy ani pustych linii w Render

# 5. Po dodaniu zmiennych, redeploy serwis
