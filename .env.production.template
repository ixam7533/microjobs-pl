# Production Environment Variables for Railway/Render
# Copy these variables to your hosting platform's Environment Variables section

# === REQUIRED VARIABLES ===

# Database (PostgreSQL on production)
DATABASE_URL=postgresql://username:password@hostname:port/database

# JWT Secret (generate strong random key)
JWT_SECRET=your-super-strong-jwt-secret-key-here

# NextAuth Secret (generate strong random key) 
NEXTAUTH_SECRET=your-super-strong-nextauth-secret-key-here

# Application URL (replace with your actual domain)
# For Railway: https://your-app-name.up.railway.app
# For Render: https://your-app-name.onrender.com
NEXTAUTH_URL=https://your-app-domain-here

# Environment
NODE_ENV=production

# Email (already configured)
SMTP_USER=unlimitedcontentg@gmail.com
SMTP_PASS=your-smtp-app-password

# === OPTIONAL VARIABLES (OAuth) ===

# Google OAuth (for "Continue with Google" button)
# Get from: https://console.cloud.google.com
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Facebook OAuth (for "Continue with Facebook" button)
# Get from: https://developers.facebook.com  
FACEBOOK_CLIENT_ID=your-facebook-client-id
FACEBOOK_CLIENT_SECRET=your-facebook-client-secret

# === INSTRUCTIONS ===
# 1. Set these in your hosting platform's environment variables
# 2. For OAuth, configure redirect URLs:
#    - Google: https://your-domain/api/auth/callback/google
#    - Facebook: https://your-domain/api/auth/callback/facebook  
# 3. Test configuration: https://your-domain/api/debug/oauth-status
# 4. Without OAuth, email/password login will still work

# === INSTRUKCJE ===

# 1. Wygeneruj silne klucze:
#    openssl rand -base64 32

# 2. W Render, przejdź do:
#    Dashboard > Your Service > Environment

# 3. Dodaj każdą zmienną osobno jako Key-Value pair

# 4. Nie dodawaj komentarzy ani pustych linii w Render

# 5. Po dodaniu zmiennych, redeploy serwis
